import { Eg8HighlightDirective } from './eg8-highlight.directive';

describe('Eg8HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new Eg8HighlightDirective();
    expect(directive).toBeTruthy();
  });
});
